ctrl + v ; {u,d,m,t,U, D}
    gac {up,down,mute,unmute,up 16, down 16}

alt + b
    headset

ctrl + k ; {p,i}
    setxkbmap -layout {br, us -option caps:escape} 

ctrl + s ; {l, u, d, U, D, m, o}
    {slock, gbc up, gbc down, gbc upp, gbc downn, gbc max, poweroff} 
